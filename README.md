# 🎾 테니스윗 분기 대회 일정표

빌드 없는 정적 웹 앱 (HTML/CSS/JS). GitHub Pages 로 배포됩니다.
→ https://koozinsong.github.io/tennisweet/

## 사용 흐름 (관리자)
1. 상단 **🔐 관리자** 버튼 → 비밀번호 입력 (편집 모드 진입, NTRP 표시)
2. **① 선수** — 이름·성별·NTRP·합류/퇴장 시각 입력, 참가 여부 체크. 여러 명 일괄 추가: `이름, 성별, NTRP, 합류시각` 한 줄에 한 명
3. **② 대회 설정** — 팀전 / 개인전 / 복식 로테이션, 팀 수, 코트 수, 시작·종료 시각, 경기 시간
4. **③ 팀 구성** — 균등 배정(합류 시각·NTRP·성별 고려) 후 드롭다운으로 수동 조정
5. **④ 일정표** — 전체 일정 생성, 게임 스코어 입력. **현장 편집 모드**: 시간·코트 이동, 참가자 교체, 경기 추가/삭제, 충돌(⚠) 경고
6. **⑤ 순위·대진** — 팀 순위(복식 승수→득실차) + 개인 기록
- 선수가 바뀌면 **🔄 팀 배정·일정 다시 생성** 한 번으로 재생성

## 팀전 배정 규칙
- 합류 시각 구간별로 2명(강+약)씩 짝지어 팀에 넣어, 각 팀이 그 시간대에 복식 인원을 갖추도록 함 (코트 최대 활용)
- 팀 규모 → 구간 인원 → 여성 수 → 평균 NTRP 순으로 균형
- 일정: 시간대마다 코트별 팀 대 팀 복식 1경기. 경기 수 적은 선수 우선, 파트너 중복 최소, 같은 시간대 중복 배정 금지

## 데이터와 공유
- **원본 = `data/tournament.json`**. 페이지를 여는 모두가 이 게시본을 읽기 전용으로 봅니다 (선수 명단은 누구나 볼 수 있고 NTRP 열만 관리자에게 표시).
- 관리자 편집 내용은 브라우저(localStorage)에만 저장됩니다. 게시하려면 **내보내기** → 받은 `tournament.json` 을 `data/` 에 덮어쓰고 `git push`. (또는 Claude Code 로 파일 직접 수정 후 push)
- 현장에서 즉시 공유: **🔗 공유 링크** — 현재 상태 전체를 링크에 담아 카톡 등으로 전달 (읽기 전용 스냅샷)
- **NTRP 는 관리자 비밀번호로 AES-GCM 암호화**되어 저장됩니다. 저장소 JSON 을 열어도 평문이 보이지 않습니다. 비밀번호 변경은 `app.js` 의 `ADMIN_HASH`(sha256(`tennisweet-v1` + 비밀번호)) 를 바꾸고, 기존 NTRP 는 새 비밀번호로 다시 입력해야 합니다.

## 로컬 실행
```bash
python3 -m http.server 8080
```
`http://localhost:8080` 접속.

## 배포
`main` 에 push 하면 `.github/workflows/pages.yml` 이 사이트를 `gh-pages` 브랜치로 게시하고, GitHub Pages 가 그 브랜치를 서빙합니다. (Settings → Pages 의 Source 가 `gh-pages` / root 인지 한 번만 확인)
